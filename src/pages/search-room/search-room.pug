extends ../../templates/website-pages

include ../../components/layout/layout
include ../../components/room-list/room-list
include ../../components/filter/filter

block title 
  | Search room

block main
  +layout__box({isContainer: true, grow: 1, mt: 30})
    +layout__box
      +layout__box({basis: 266, shrink: '0'})
        +filter
      +layout__box({basis: 834, align: 'right'})
        +room-list