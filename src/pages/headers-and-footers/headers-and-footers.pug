extends ../../templates/ui-kit

include ../../components/header/header
include ../../components/footer/footer
include ../../components/wrapper/wrapper
include ../../components/box/box

block var
  -
    var withThemeGray = true

block title
  | Headers and footers

block main
  +wrapper({marginTop: 40})
    +header 
  +wrapper({marginTop: 40})
    +header({username: 'Юлий Цезарь'})
  +wrapper({marginTop: 40})
    +footer
  +wrapper({marginTop: 40, marginBottom: 140})
    +box({width: 768})
      +footer({mobile: true})
