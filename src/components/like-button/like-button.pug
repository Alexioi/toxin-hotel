mixin like-button(options)
  -
    const {
      isLiked = false, 
      counter = 0
    } = options || {}

    let classes = ''
    classes += isLiked && ' like-button_liked'

  button.like-button.js-like-button(class=classes type='button')
    .like-button__icon 
    span.like-button__counter.js-like-button__counter= counter
