mixin dropdown-field(options)
  -
    const {
      inputName = ''
    } = options || {}

  .dropdown-field
    input.dropdown-field__input(name=inputName)
    button.dropdown-field__button(type="button")
