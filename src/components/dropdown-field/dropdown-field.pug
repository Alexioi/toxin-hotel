mixin dropdown-field(options)
  -
    const {
      inputName = ''
    } = options || {}

  .dropdown-field
    input.dropdown-field__input(name=inputName readonly)
    button.dropdown-field__button(type="button")