mixin rate(options)
  -
    const { quantity = 0 } = options || {};
    let i = 0;

  .rate
    while i < 5
      -
        const starType = quantity > i ? 'star' : 'outline-star'
        i += 1
        const svg = require(`!raw-loader!@images/content/${starType}.svg`).default;

      != svg
