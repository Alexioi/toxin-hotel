mixin label(options)
  -
    const {
      title='',
      hint=''
    } = options || {}

  label.label 
    .label__header
      span.label__title= title
      if hint !== ''
        span.label__hint= hint
    block
