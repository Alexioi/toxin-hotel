mixin toggle(options)
  -
    const {
      text,
      isChecked = false
    } = options || {}

  label.toggle
    input.toggle__checkbox(type="checkbox", checked=isChecked) 
    span.toggle__icon
    span.toggle__text= text
