mixin logo(options)
  -
    const {
      type = ''
    } = options || {}

  a.logo(href = "./index.html") 
    img(
      class = 'logo__img'
      src = require(`Components/logo/img/${type}.svg`)
      alt = "Toxin"
    )
